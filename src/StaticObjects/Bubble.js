
export default class Bubble extends Phaser.GameObjects.Sprite {
  
    /*constructor(scene, x, y, nameImg){
      super(scene, x,y,nameImg);
      this.scene.add.existing(this);
      this.scene.physics.add.existing(this);
      this.body.setCollideWorldBounds(true);
      this.body.setAllowGravity(true);

      this.body.setBounce(1);

      this.x=Phaser.Math.Between(300,900);
      this.y=Phaser.Math.Between(0,500);

      this.rndX=Phaser.Math.Between(-1,1);
      this.rndY=Phaser.Math.Between(-1,1);

      this.speed=400;

      this.calculateVelocity();
      
    }
    stopX() {
        this.body.setVelocityX(0);
    }

    calculateVelocity() {

        this.body.setVelocityX(this.rndX * this.speed);
        this.body.setVelocityY(this.rndY*this.speed);
      }*/

      //Por si necesito una burbuja
    
  }
  